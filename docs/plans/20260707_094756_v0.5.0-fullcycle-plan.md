# v0.5.0 Repository Hardening — Full Lifecycle Plan

> **For agentic workers:** This plan maps the ExecForge lifecycle (`c-level` router order) onto the v0.5.0 repository-hardening job. Stages marked DONE list their actual evidence; never re-claim them. Stages marked PENDING are the remaining execution path. Steps use checkbox (`- [ ]`) syntax for tracking.

**Initiative:** Repository hardening and first tagged release of the ExecForge skill bundle (v0.5.0, commit `4123e3d`, tag `v0.5.0`).

**Lifecycle:** Product decision → upstream approval → (design bridge) → engineering plan → implementation → Staff Engineer review → QA gate → delta review → final ship decision.

**Honesty rule:** A stage counts only if its artifact or transcript evidence exists. This plan explicitly records which formal stages were skipped during the original execution.

---

## Stage 0 — Product decision (`execforge`) — DONE (informal)

- **What happened:** The user directly specified six improvements (skill structure, README, progressive references, install/dependency validation, evaluations + CI, license + tagged release). Direct user instructions rank above the skills, so no CEO/COO pressure test was run — the scope was user-decided.
- **Evidence:** User request of 2026-07-06; approved plan `~/.claude/plans/misty-weaving-shell.md`.
- **Formal gap:** No `.execforge/` decision record exists.
- [ ] Optional backfill: record a one-page decision memo (`.execforge/runs/<ts>-v050-hardening/`) noting scope, verdict `GO`, and evidence, clearly marked *retroactive*.

## Stage 1 — Upstream approval gate (`eng-level` stop check) — DONE (informal)

- **What happened:** The interpreted scope was presented as a plan-mode plan; the user reviewed it (including an Ultraplan refinement round) and approved it. Ambiguities ("clo" = CI; release scope = push + GitHub Release) were resolved with explicit user answers.
- **Evidence:** Approved plan file; AskUserQuestion answers in session transcript.
- **Formal gap:** No `.eng-level/upstream-requirements.md` / `upstream-approval.md` artifacts.
- [ ] Optional backfill: write the six requirements + user approval into `.eng-level/upstream-requirements.md`, marked *retroactive*.

## Stage 2 — UI/UX design bridge (`design-html`) — NOT APPLICABLE

- No user-facing interface was in scope (docs site uses stock MkDocs theme). No action.

## Stage 3 — Engineering plan review (`eng-level --mode=plan`) — DONE (informal)

- **What happened:** A 7-task implementation plan with files, approach, and a verification section was written and user-approved before implementation. No independent plan-engineering review (gstack or fallback) was run against it.
- **Evidence:** `~/.claude/plans/misty-weaving-shell.md`; task list #1–#7 all completed.
- **Formal gap:** No `.eng-level/plan-review.md` verdict.

## Stage 4 — Implementation — DONE

- **Evidence:** Commit `4123e3d` on `main`: 23 files changed (+836 / −2,873). Legacy monoliths removed, 4 new reference files, `doctor` + install verification, `evaluations/` (6 files), 2 workflows, README rewrite, test extensions, CHANGELOG 0.5.0.

## Stage 5 — Verification — DONE

- **Evidence (all actually run, 2026-07-06):**
  - `python3 scripts/execforge.py validate` → passed
  - `python3 scripts/execforge.py doctor` → no blocking problems
  - `python3 -m unittest discover -s tests` → 13/13 OK
  - `mkdocs build --strict` (venv) → clean
  - GitHub Actions CI run `28776823077` → success; docs deploy `28776822905` → success after Pages enablement
  - Install verified to claude/codex/agents targets (15 skill copies)

## Stage 6 — Staff Engineer review (`eng-level --mode=review`) — PENDING

The formal whole-branch audit never ran. This is the highest-value remaining stage.

- [ ] Run `eng-level --mode=review` against diff `71fb441..v0.5.0` with the approved plan as baseline.
- [ ] Cover: correctness of `doctor`/`verify_installed_skill`/validator changes, workflow syntax and permissions, README claim accuracy, test realism, conformance of all 7 plan tasks, scope drift.
- [ ] Record findings with P0–P3 severity in `.eng-level/staff-review.md` and conformance in `.eng-level/conformance.md`.

## Stage 7 — QA gate (`q-level`) — PENDING (reduced scope)

No portal/API/backend transaction exists; the QA surface is the CLI behavior contract. Run a reduced, honest gate rather than claiming full cross-layer QA:

- [ ] QA plan: scenarios for `validate` (clean + seeded-defect repo), `doctor` (healthy + broken env), `install` (fresh, existing-without-force, `--force`, corrupted bundle refusal), `init-run`, `status`.
- [ ] Execute against a throwaway copy of the repo; record evidence per scenario in `.q-level/execution-evidence.md`.
- [ ] Verdict `QA PASS / RETURN / BLOCK` in `.q-level/decision.md`; route any code defect back to implementation.

## Stage 8 — Delta review and retest loop — CONDITIONAL

- [ ] If Stage 6 or 7 produces fixes that change shipped code: apply on a branch, re-run Staff delta review + affected QA scenarios, and release as `v0.5.1` with a CHANGELOG entry. Never amend the published `v0.5.0` tag.

## Stage 9 — Final engineering decision — PENDING

- [ ] Issue exactly one verdict in `.eng-level/decision.md`: `SHIP` (ratifying v0.5.0 as-is), `SHIP WITH REQUIRED FIXES` (owned P2/P3 → v0.5.1 backlog), or `RETURN TO IMPLEMENTATION` (P0/P1 found → v0.5.1 required).
- [ ] Reconcile the Ultraplan cloud PR (session `01FGVwbSUm5NohLTZ9uEi4YF`) when it lands: compare against `main`, salvage anything better, close if redundant.

---

## Execution order

1. Stage 6 (Staff review audit) — informs everything downstream.
2. Stage 7 (CLI QA gate).
3. Stage 8 only if defects found; Stage 9 always.
4. Optional Stage 0/1 backfill memos last (documentation, not gating).

## Stop conditions

- Stop and return to the user if the audit finds a P0/P1 in the released tag (release-note or v0.5.1 decision is the user's call).
- Never claim a stage above as run without its artifact existing.
